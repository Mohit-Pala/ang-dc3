specVersion: "v1beta"
serviceId: "ang-dataconnect"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-cloud-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
